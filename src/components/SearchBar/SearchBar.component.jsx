import React from 'react';
import './SearchBar.styles.scss';

const SearchBar = () => {
  return(
    <input type='search' placeholder='Search a movie or tv-show...'></input>
  )
}

export default SearchBar;